export type Game = {
  board: string[][];
  initialBoard: string[][];
  createdAt: string;
  difficulty: string;
  gameState: string;
  name: string;
  updatedAt: string;
  uuid: string;
  bitmap?: string;
  bitmapUrl?: string;
};
